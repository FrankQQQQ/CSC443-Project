# CSC443-Project
Final project for CSC443

To run in command line:
```shell
cmake . -DCMAKE_VERBOSE_MAKEFILE=ON &&
make &&
./KVStoreCommandLine
```

To test:
```shell
cmake . -DCMAKE_VERBOSE_MAKEFILE=ON &&
make &&
make test
```
